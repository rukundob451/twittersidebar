import 'package:flutter/material.dart';

class LISTS extends StatelessWidget {
  const LISTS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('This are the lists'),
        centerTitle: true,
      ),
    );
  }
}
