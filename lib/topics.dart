import 'package:flutter/material.dart';

class TOPICS extends StatelessWidget {
  const TOPICS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('These are the topics'),
        centerTitle: true,
      ),
    );
  }
}
