import 'package:flutter/material.dart';

class MOMENTS extends StatelessWidget {
  const MOMENTS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('These are moments'),
        centerTitle: true,
      ),
    );
  }
}
