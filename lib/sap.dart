import 'package:flutter/material.dart';

class SAP extends StatelessWidget {
  const SAP({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('These are the settings and privacy'),
        centerTitle: true,
      ),
    );
  }
}
